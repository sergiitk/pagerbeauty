box: node:10.11.0-alpine
build:
  steps:
    - script:
        name: report yarn version
        code: yarn --version
    - script:
        name: set yarn cache
        code: yarn config set cache-folder $WERCKER_CACHE_DIR/yarn
    - script:
        name: install dependencies
        code: yarn
    - script:
        name: lint
        code: yarn lint
    - script:
        name: tests
        code: yarn test:full
